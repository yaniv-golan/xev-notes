{
  "name": "search-notes",
  "description": "Search Evernote notes by query. Returns matching notes with titles, notebooks, dates, and note links. Use get-note to retrieve full content.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "query": {"type": "string", "description": "Search query (supports Evernote search grammar)"},
      "notebook": {"type": "string", "description": "Restrict to a specific notebook name"},
      "limit": {"type": "integer", "default": 20, "description": "Max number of results"}
    },
    "required": ["query"]
  }
}
